<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vv.beaver">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Beaver.BeaverListActivity" />
        <activity android:name=".Menu.MenuListActivity" />
        <activity android:name=".Beaver.CurrentBeaverActivity" />
        <activity android:name=".StartupLogoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Beaver.NewBeaverActivity" />
        <activity android:name=".Menu.ItemsMenuActicity" />
        <activity android:name=".Test.TestActivity"></activity>
    </application>

</manifest>